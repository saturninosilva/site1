let numeros = [
    '10',
    '27',
    '30',
    '44',
    '36'
];

//map
function adicionaTres (item, indice, lista){
    let soma = item + 3;
    return item(`${indice} - ${item} - ${lista}`);

console.log(item);
}